/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: ['49.13.154.79'] // Add the hostname of your image source here
	},
	output: 'standalone'
}

export default nextConfig
