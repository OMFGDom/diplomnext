/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: ['api.curriculum-crafter.me'] // Add the hostname of your image source here
	},
	output: 'standalone'
}

export default nextConfig
